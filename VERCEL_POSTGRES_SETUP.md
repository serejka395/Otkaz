# VERCEL POSTGRES SETUP GUIDE

## 1️⃣ Создание Vercel Postgres базы данных

1. Перейди на https://vercel.com/dashboard
2. Выбери свой проект "Otkaz"
3. Перейди в **Storage** → **Create Database** → **Postgres**
4. Выбери регион (рекомендуется близко к пользователям)
5. Нажми **Create**

## 2️⃣ Получение Connection String

После создания БД:
1. Нажми на БД в Storage
2. Скопируй **POSTGRES_PRISMA_URL** (с pooling)
3. Скопируй **POSTGRES_URL_NON_POOLING** (без pooling, для миграций)

## 3️⃣ Добавление Environment Variables в Vercel

1. В проекте перейди в **Settings** → **Environment Variables**
2. Добавь две переменные:
   - **POSTGRES_PRISMA_URL** = (скопированный Connection String)
   - **POSTGRES_URL_NON_POOLING** = (скопированный Connection String)
3. Убедись что выбраны все окружения: Production, Preview, Development
4. Нажми **Save**

## 4️⃣ Локальное тестирование (опционально)

Если хочешь тестировать локально:
1. Обнови .env.local с реальными значениями от Vercel:
```
POSTGRES_PRISMA_URL="postgresql://..."
POSTGRES_URL_NON_POOLING="postgresql://..."
```

2. Установи зависимости:
```bash
npm install
```

3. Запусти миграции:
```bash
npm run db:push
```

4. (Опционально) Заполни начальные данные:
```bash
npm run db:seed
```

## 5️⃣ Автоматическое развёртывание на Vercel

1. Запусти `git push` в свой репозиторий
2. Vercel автоматически:
   - Установит зависимости
   - Запустит миграции (через `prisma migrate deploy` в build script)
   - Задеплоит приложение

## 6️⃣ Проверка что всё работает

После деплоя:
1. Перейди на свой деплой URL
2. Попробуй создать аккаунт
3. Проверь что данные сохраняются в Vercel Postgres

## ⚠️ Важные замечания

- **POSTGRES_PRISMA_URL** - используется для приложения (с pooling connection, быстрее)
- **POSTGRES_URL_NON_POOLING** - используется для миграций (directUrl в schema.prisma)
- Обе переменные должны быть **одинаковыми** для локального тестирования
- Бесплатный Vercel Postgres имеет лимиты - смотри pricing

## 🔄 Откаты и повторные миграции

Если что-то пошло не так:
1. В Vercel dashboard перейди в Storage → твоя БД
2. Смотри Query Logs для отладки
3. Если нужно стереть все - удали БД и создай новую

## 📝 Переменные в коде

Если нужно использовать Vercel Postgres напрямую в коде (минуя Prisma):
```typescript
import { sql } from '@vercel/postgres';

const { rows } = await sql`SELECT * FROM "User" LIMIT 1`;
```

Но обычно это не нужно - используй Prisma client.
