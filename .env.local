# Vercel Postgres - эти переменные автоматически добавятся после подключения базы данных в Vercel
# Для локальной разработки заполните вручную после создания базы данных в Vercel
POSTGRES_PRISMA_URL="postgresql://user:password@host/database?sslmode=require&pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host/database?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="your-random-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Telegram Bot
NEXT_PUBLIC_BOT_USERNAME="enough_appbot"
